# tun2socks runtime

This folder contains the Windows x64 tunnel runtime used by GrowStone Bot.

- `tun2socks.exe` comes from `xjasonlyu/tun2socks`.
- `wintun.dll` comes from WireGuard Wintun.

The app expects both files in this folder and must run as Administrator.
